
cport      REAL*4    opt_time /24/           ! I) durata dell'ottimizzazione
      REAL*4    opt_time            ! I) durata dell'ottimizzazione
!      REAL*4    start_cost(max_unit)   ! I) costo di accensione delle unita''
!     *         ,shut_cost(max_unit)    ! I) costo di spegnimento  "     "

      COMMON /ARGO_TIME/ opt_time
!     *                 ,start_cost,shut_cost
