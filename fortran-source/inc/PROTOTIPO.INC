       real*4 cmin_eff(maxunits)  ! curve limite effettive
       real*4 cmax_eff(maxunits)  ! curve limite effettive
       real*4 clim_new(maxunits)  ! curva limita di antipompaggio come
	 			  ! parabola H/Q2
       real*4 clim2_new(maxunits) ! curva limita di antipompaggio come
       common/prototi/clim_new,cmin_eff,cmax_eff,clim2_new
